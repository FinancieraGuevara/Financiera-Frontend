import { DetallePrestamo } from '../../Clases/detallePrestamo/detalle-prestamo';

describe('DetallePrestamo', () => {
  it('should create an instance', () => {
    expect(new DetallePrestamo()).toBeTruthy();
  });
});
